The program demonstrates:
- User input with `input()`
- Type conversion using `int()`
- Using `abs()` to ensure age is positive
- Output formatting with `print()`
- Multi-line text using `\
- Welcomes the user.
- Asks for the user's name.
- Requests the user's age.
- Displays sample height and favorite number prompts.
- Shows a summary of the collected information.
- Displays an approximate birth year based on the age entered.
- Ends with a thank-you message.



















































































































































































































